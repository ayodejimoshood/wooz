
import {

} from '../actions/types';



const initialState = {
  user: 'sulaimon'
}

export default function(state = initialState, action){
  switch (action.type) {
  
    default:
      return state;
  }
}